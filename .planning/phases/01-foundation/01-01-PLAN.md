---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Create Maven parent POM and core module to establish Bill's project foundation.

Purpose: Bootstrap the project structure with proper Maven configuration and create the foundational core module that subsequent modules will depend on.
Output: Working Maven parent POM with bill-core module, buildable with `mvn clean compile`.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

**Tech stack:**
- Java 25+ (target JDK version)
- Maven (bootstrap build tool)
- TestNG (test framework)

**Key constraints from CONTEXT.md:**
- Follow standard Maven conventions throughout
- Use typical Maven directory layouts (src/main/java, src/test/java)
- Standard parent POM patterns
- Clean module boundaries with single, obvious purpose per module
- Create only modules needed for Phases 1-3 initially

**Key decisions from PROJECT.md:**
- JDK 25+ target for latest language features
- Maven for bootstrap until self-hosting
- TestNG for Bill's own test suite
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Maven parent POM</name>
  <files>pom.xml</files>
  <action>Create parent POM in project root with:
- Project coordinates: groupId=com.example.bill, artifactId=bill-parent, version=0.1.0-SNAPSHOT
- Packaging type: pom
- Properties: maven.compiler.source=25, maven.compiler.target=25, project.build.sourceEncoding=UTF-8
- Modules section (initially empty, will add bill-core in Task 2)
- Dependency management section with TestNG (version 7.10.2) in test scope
- Standard plugins: maven-compiler-plugin (3.13.0), maven-surefire-plugin (3.5.2)
- Do NOT add plugins for code coverage, formatting, or other concerns yet - keep it minimal for Phase 1</action>
  <verify>mvn validate succeeds, shows valid POM structure</verify>
  <done>pom.xml exists, validates successfully, has JDK 25 configuration and TestNG dependency management</done>
</task>

<task type="auto">
  <name>Task 2: Create bill-core module</name>
  <files>bill-core/pom.xml, bill-core/src/main/java/com/example/bill/core/package-info.java, bill-core/src/test/java/com/example/bill/core/package-info.java</files>
  <action>Create bill-core module:
1. Create bill-core/pom.xml with:
   - Parent reference to bill-parent
   - ArtifactId: bill-core
   - TestNG dependency in test scope (inherited version from parent)
   - No additional dependencies yet (will add as needed in later phases)
2. Create directory structure: bill-core/src/main/java/com/example/bill/core/ and bill-core/src/test/java/com/example/bill/core/
3. Add package-info.java in both main and test with simple package documentation: "Core utilities and shared foundations for Bill build system."
4. Update parent pom.xml to add bill-core to modules section

Follow standard Maven conventions - nothing unconventional.</action>
  <verify>mvn clean compile succeeds, bill-core module compiles without errors</verify>
  <done>bill-core module exists with proper Maven structure, compiles successfully, parent POM references it in modules section</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn validate` succeeds for parent POM
- [ ] `mvn clean compile` succeeds for entire project
- [ ] No compilation errors or warnings
- [ ] bill-core module has proper package structure
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Parent POM establishes JDK 25 target and TestNG dependency management
- bill-core module created with clean Maven structure
- Project builds successfully end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

---
phase: 01-foundation
plan: 01
subsystem: build-foundation
requires: []
provides: [maven-parent, bill-core]
affects: [01-02]
tags: [maven, project-structure]
key-decisions: []
key-files: [pom.xml, bill-core/pom.xml]
tech-stack:
  added: [maven-parent-pom, bill-core-module]
  patterns: [standard-maven-layout]
patterns-established: [maven-multi-module, jdk-25-target]
---

# Phase 1 Plan 1: Maven Parent & Core Module Summary

**Substantive one-liner:** Established Maven parent POM with JDK 25 configuration and created bill-core foundation module.

## Accomplishments

- Created Maven parent POM with project coordinates and JDK 25 target
- Configured TestNG dependency management for test framework
- Created bill-core module with standard Maven directory structure
- Verified end-to-end compilation works

## Files Created/Modified

- `pom.xml` - Maven parent POM with JDK 25 config, dependency management
- `bill-core/pom.xml` - Core module POM
- `bill-core/src/main/java/com/example/bill/core/package-info.java` - Main package documentation
- `bill-core/src/test/java/com/example/bill/core/package-info.java` - Test package documentation

## Decisions Made

[Document any decisions made during implementation, or "None"]

## Issues Encountered

[Document any problems and resolutions, or "None"]

## Next Phase Readiness

Ready for Plan 01-02 (Module Structure & Verification). The Maven foundation is in place for adding additional modules.
</output>
